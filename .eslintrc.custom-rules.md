# カスタムESLintルール（将来実装用）

現在は手動チェックが必要ですが、将来的にESLintプラグインとして実装すべきルール

## 1. z-indexの直接指定を禁止

```typescript
// ❌ 禁止
<div className="z-50">...</div>

// ✅ 推奨
import { Z_INDEX } from '@/lib/constants/z-index';
<div style={{ zIndex: Z_INDEX.MODAL }}>...</div>
```

**理由**: z-indexの一貫性を保つため

---

## 2. padding値の統一チェック

```typescript
// ❌ 非推奨
className="pt-16 sm:pt-20"  // 古いpadding
className="px-2 sm:px-4"    // 狭すぎる

// ✅ 推奨
className="pt-20 sm:pt-24"  // GameHeader対応
className="px-3 sm:px-4"    // 適切な余白
```

**理由**: レイアウトの一貫性とスマホでの余白確保

---

## 3. fixed/absolute使用時のチェック

```typescript
// ❌ 警告: z-indexとpointer-eventsを確認
<div className="fixed inset-0">...</div>

// ✅ OK: 必要な属性が揃っている
<div 
  className="fixed inset-0 pointer-events-none" 
  style={{ zIndex: Z_INDEX.OVERLAY }}
>
  <div className="pointer-events-auto">...</div>
</div>
```

**チェック項目**:
- z-indexが定数で指定されているか
- オーバーレイの場合、pointer-events-noneが指定されているか
- 子要素にpointer-events-autoが必要か

---

## 実装方法（将来）

1. ESLintプラグインの作成
2. または、Biomeルールとして実装
3. または、pre-commit hookでgrepチェック

### pre-commit hookの例

```bash
#!/bin/bash
# .git/hooks/pre-commit

# z-indexの直接指定をチェック
if git diff --cached --name-only | xargs grep -l 'className.*z-[0-9]'; then
  echo "❌ Error: z-index should use Z_INDEX constants"
  echo "See /lib/constants/z-index.ts"
  exit 1
fi

# 古いpadding値をチェック
if git diff --cached --name-only | xargs grep -l 'pt-16 sm:pt-20'; then
  echo "⚠️  Warning: Use pt-20 sm:pt-24 for consistency"
  echo "See docs/ui-layout-guidelines.md"
fi
```

---

**更新日: 2026-06-02**
